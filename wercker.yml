box: wercker/ruby
# Build definition
build:
  # The steps that will be executed on build
  steps:
    - script:
        name: install jruby
        code: rvm install jruby
    - script:
        name: run rake
        code: rake
